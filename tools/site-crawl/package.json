{
  "name": "site-crawl",
  "version": "0.1.0",
  "description": "One JSON inventory of the URLs a bounded polite crawl can reach from one start URL",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/site-crawl.js help",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
