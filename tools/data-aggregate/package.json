{
  "name": "data-aggregate",
  "version": "0.1.0",
  "description": "Groups a CSV, JSON, or TSV file's rows by one or more columns and computes sum, mean, median, min, max, or count over named metric columns",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/aggregate.js help",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
