# Third-party notice: seo-data

**This tool installs nothing.** It declares no npm dependency and creates no `node_modules`, so no package is fetched when it runs. Node's standard library covers every command.

This repository redistributes no third-party package with this tool.
