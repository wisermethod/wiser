{
  "name": "image",
  "version": "0.3.0",
  "description": "Applies local edits to an existing image or composites an overlay over a base image, and writes the result as a PNG, JPEG, or WEBP",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/image.js help",
    "test": "node --test"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
