{
  "name": "data-parse",
  "version": "0.1.1",
  "description": "Parses a CSV, JSON, or TSV file into a column profile with detected types, non-null counts, sample values, row count, uneven-row count, and parse errors",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/parse.js help",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
