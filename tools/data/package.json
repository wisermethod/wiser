{
  "name": "data",
  "version": "0.2.0",
  "description": "Parse, describe, aggregate, join, and chart CSV, JSON, or TSV files, and compute a percentage, difference, or rate from two numeric fields of a JSON object",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/data.js help",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
