{
  "name": "data-describe",
  "version": "0.1.1",
  "description": "Descriptive statistics for the numeric columns of a CSV, JSON, or TSV file: count, mean, median, min, max, standard deviation, p25, p75, and null count",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/describe.js help",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
