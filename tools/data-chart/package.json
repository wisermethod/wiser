{
  "name": "data-chart",
  "version": "0.1.0",
  "description": "Builds a self-contained HTML file with an SVG bar or line chart from a CSV, JSON, or TSV file",
  "private": true,
  "type": "module",
  "scripts": {
    "help": "node scripts/chart.js help",
    "test": "node --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
