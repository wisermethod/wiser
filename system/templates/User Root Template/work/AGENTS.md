# Work

Projects with a written completion condition live at `work/<slug>/`. Unsure defaults here. A standing concern without a completion condition belongs in programs. This router projects `standards/user-root.md` C4.

`onboarding/` holds root-creation records under its router. `zArchive/` is the work-deliverable archive under C11. A work slug's `AGENTS.md` names its files and its completion condition.
