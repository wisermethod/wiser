# Programs

Standing concerns with no completion condition live at `programs/<slug>/`. A project with a written completion condition lives under its program at `programs/<slug>/<project>/`, or at `work/<slug>/` without a program. Unsure defaults to work. This router projects `standards/user-root.md` C4. Do not create slug folders before a concern is named.
