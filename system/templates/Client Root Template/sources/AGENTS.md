# Sources

Documents supplied for this client, as received. This is classified input, not this root's output (`work/`) and not the waiting room (`inbox/`). Do not edit in place. Derived extracts live in `extraction/` under this root's records home, the one its `AGENTS.md` declares.

File by the source's own name. When this folder holds subfolders (by brand, by workstream, by date), this file names them.
