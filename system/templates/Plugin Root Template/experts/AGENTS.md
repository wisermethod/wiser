# Experts

Personas that carry a perspective, judge work through it, and sequence the skills a chain of work needs; `wiser/standards/primitives.md` owns the type's definition, invocation rules, frontmatter, and the classifier seam every new typed file here, and any file whose seam changes, declares in its Context. The directory is flat, and the index below is grouped by category. Ownership is projected from each expert's `Owns:` line and from a skill's `Stands alone:`, per that standard's Indexes. This index is the routing table `wiser/AGENTS.md` Precedence and routing names once it has rows.

This index is empty. This family holds no primitive yet.
